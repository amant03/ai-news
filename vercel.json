{
  "ignoreCommand": "git log -1 --pretty=%s | grep -qE '^chore: refresh' && echo skip-news-commit || exit 1",
  "crons": [
    {
      "path": "/api/cron",
      "schedule": "0 9 * * *"
    }
  ],
  "env": {
    "PUPPETEER_SKIP_DOWNLOAD": "true",
    "DATA_REPO": "amant03/ai-news",
    "DATA_BRANCH": "main"
  }
}
